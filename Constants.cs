namespace Trigonometry;

public static class Constants
{
    public const float DEG_TO_RAD = (float)(Math.PI / 180);
    public const float RAD_TO_DEG = (float)(180 / Math.PI);
    public const float POSITIVE_HALF_PI = (float)(Math.PI / 2);
    public const float NEGATIVE_HALF_PI = -POSITIVE_HALF_PI;
}
